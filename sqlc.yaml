version: "2"
sql:
  - engine: sqlite
    schema: migrations/001_initial_schema.sql
    queries: backend/storage/queries
    gen:
      go:
        package: db
        out: backend/storage/db
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: false
        emit_exact_table_names: false
